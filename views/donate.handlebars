<div class="project-detail-hero">
    <div class="hero-background">
        <img src="{{project.image}}" alt="{{project.title}}">
        <div class="hero-overlay"></div>
    </div>
    <div class="hero-content">
        <div class="breadcrumb">
            <a href="/">Home</a> → <a href="/#projects">Projects</a> → <span>{{project.title}}</span>
        </div>
        <h1 class="project-hero-title">{{project.title}}</h1>
        <p class="project-hero-subtitle">{{project.subtitle}}</p>
        <div class="hero-stats">
            <div class="stat-item">
                <span class="stat-number">{{project.raisedFormatted}}</span>
                <span class="stat-label">Raised</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">{{project.percentage}}%</span>
                <span class="stat-label">Complete</span>
            </div>
            <div class="stat-item">
                <span class="stat-number">{{project.goalFormatted}}</span>
                <span class="stat-label">Goal</span>
            </div>
        </div>
    </div>
</div>

<div class="project-detail-content">
    <div class="container">
        <div class="content-grid">
            <div class="main-content">
                <div class="project-story">
                    <h2>The Full Story</h2>
                    <div class="story-content">
                        {{#each (split project.fullDescription '\n\n')}}
                        <p>{{this}}</p>
                        {{/each}}
                    </div>
                </div>
                
                <div class="project-impact">
                    <h2>Impact & Goals</h2>
                    <div class="impact-grid">
                        <div class="impact-card">
                            <div class="impact-icon">🏠</div>
                            <h3>Direct Impact</h3>
                            <p>{{project.impact}}</p>
                        </div>
                        <div class="impact-card">
                            <div class="impact-icon">👥</div>
                            <h3>Community Reach</h3>
                            <p>Serving 100+ families in the local community</p>
                        </div>
                        <div class="impact-card">
                            <div class="impact-icon">⏰</div>
                            <h3>Timeline</h3>
                            <p>Expected completion in 6-12 months</p>
                        </div>
                    </div>
                </div>
                
                <div class="key-points-detail">
                    <h2>What Your Donation Achieves</h2>
                    <div class="points-list">
                        {{#each project.keyPoints}}
                        <div class="point-item">
                            <div class="point-check">✓</div>
                            <div class="point-content">
                                <p>{{this}}</p>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                </div>
            </div>
            
            <div class="sidebar">
                <div class="donation-widget sticky">
                    <div class="widget-header">
                        <h3>Make a Donation</h3>
                        <p>Every contribution makes a difference</p>
                    </div>
                    
                    <div class="progress-section">
                        <div class="large-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: {{project.percentage}}%"></div>
                            </div>
                            <div class="progress-stats">
                                <span class="raised">{{project.raisedFormatted}} raised</span>
                                <span class="goal">{{project.goalFormatted}} goal</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="donation-amounts">
                        <div class="amount-buttons">
                            <button class="amount-btn" data-amount="25">$25</button>
                            <button class="amount-btn" data-amount="50">$50</button>
                            <button class="amount-btn featured" data-amount="100">$100</button>
                            <button class="amount-btn" data-amount="250">$250</button>
                            <button class="amount-btn" data-amount="500">$500</button>
                            <button class="amount-btn custom">Custom</button>
                        </div>
                        <div class="custom-amount-input" style="display: none;">
                            <input type="number" placeholder="Enter amount" min="1">
                        </div>
                    </div>
                    
                    <div class="donation-form">
                        <button class="donate-now-btn">
                            <span>Donate Now</span>
                            <span class="btn-icon">❤️</span>
                        </button>
                        <p class="secure-text">🔒 Secure and encrypted donation</p>
                    </div>
                    
                    <div class="share-project">
                        <h4>Share This Project</h4>
                        <div class="share-buttons">
                            <button class="share-btn facebook">📘 Facebook</button>
                            <button class="share-btn twitter">🐦 Twitter</button>
                            <button class="share-btn whatsapp">💬 WhatsApp</button>
                            <button class="share-btn copy">🔗 Copy Link</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.project-detail-hero {
    position: relative;
    height: 60vh;
    min-height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
}

.hero-background img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.4);
}

.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.8), rgba(16, 185, 129, 0.6));
}

.hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: white;
    max-width: 800px;
    padding: 2rem;
}

.breadcrumb {
    margin-bottom: 2rem;
    font-size: 0.9rem;
    opacity: 0.9;
}

.breadcrumb a {
    color: white;
    text-decoration: none;
}

.breadcrumb a:hover {
    text-decoration: underline;
}

.project-hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    margin-bottom: 1rem;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
}

.project-hero-subtitle {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 3rem;
    opacity: 0.95;
    color: #fbbf24;
}

.hero-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-top: 2rem;
}

.stat-item {
    text-align: center;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.stat-number {
    display: block;
    font-size: 2rem;
    font-weight: 900;
    color: #fbbf24;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
}

.stat-label {
    font-size: 0.9rem;
    opacity: 0.9;
    font-weight: 500;
}

.project-detail-content {
    padding: 5rem 0;
    background: #f8fafc;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
}

.content-grid {
    display: grid;
    grid-template-columns: 1fr 350px;
    gap: 4rem;
    align-items: start;
}

.main-content {
    background: white;
    border-radius: 20px;
    padding: 3rem;
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
}

.project-story h2,
.project-impact h2,
.key-points-detail h2 {
    font-size: 2rem;
    font-weight: 700;
    color: #1f2937;
    margin-bottom: 2rem;
    position: relative;
}

.project-story h2::after,
.project-impact h2::after,
.key-points-detail h2::after {
    content: '';
    position: absolute;
    bottom: -0.5rem;
    left: 0;
    width: 60px;
    height: 4px;
    background: linear-gradient(90deg, #2563eb, #10b981);
    border-radius: 2px;
}

.story-content p {
    font-size: 1.125rem;
    line-height: 1.8;
    color: #4b5563;
    margin-bottom: 1.5rem;
}

.project-impact {
    margin-top: 4rem;
}

.impact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
}

.impact-card {
    background: linear-gradient(135deg, #f0f9ff, #e0f2fe);
    border: 1px solid #bae6fd;
    border-radius: 15px;
    padding: 2rem;
    text-align: center;
    transition: transform 0.3s ease;
}

.impact-card:hover {
    transform: translateY(-5px);
}

.impact-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
}

.impact-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    color: #1f2937;
    margin-bottom: 0.5rem;
}

.impact-card p {
    color: #6b7280;
    line-height: 1.6;
}

.key-points-detail {
    margin-top: 4rem;
}

.points-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.point-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem;
    background: #f8fafc;
    border-radius: 15px;
    border-left: 4px solid #10b981;
}

.point-check {
    background: #10b981;
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 0.8rem;
    flex-shrink: 0;
}

.point-content p {
    margin: 0;
    color: #374151;
    font-weight: 500;
}

/* Sidebar Styles */
.sidebar {
    position: relative;
}

.sticky {
    position: sticky;
    top: 2rem;
}

.donation-widget {
    background: white;
    border-radius: 20px;
    padding: 2rem;
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1);
    border: 1px solid #e5e7eb;
}

.widget-header {
    text-align: center;
    margin-bottom: 2rem;
}

.widget-header h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: #1f2937;
    margin-bottom: 0.5rem;
}

.widget-header p {
    color: #6b7280;
    font-size: 0.9rem;
}

.progress-section {
    margin-bottom: 2rem;
}

.large-progress .progress-bar {
    background: #e5e7eb;
    border-radius: 10px;
    height: 16px;
    overflow: hidden;
    position: relative;
}

.large-progress .progress-fill {
    background: linear-gradient(90deg, #10b981, #059669);
    height: 100%;
    border-radius: 10px;
    transition: width 0.8s ease;
    position: relative;
}

.large-progress .progress-fill::after {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
    animation: shimmer 2s infinite;
}

@keyframes shimmer {
    0% { left: -100%; }
    100% { left: 100%; }
}

.progress-stats {
    display: flex;
    justify-content: space-between;
    margin-top: 1rem;
    font-size: 0.9rem;
}

.raised {
    color: #10b981;
    font-weight: 600;
}

.goal {
    color: #6b7280;
}

.donation-amounts {
    margin-bottom: 2rem;
}

.amount-buttons {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.amount-btn {
    background: #f8fafc;
    border: 2px solid #e5e7eb;
    border-radius: 10px;
    padding: 0.75rem;
    font-weight: 600;
    color: #374151;
    cursor: pointer;
    transition: all 0.3s ease;
}

.amount-btn:hover {
    border-color: #2563eb;
    background: #eff6ff;
    color: #2563eb;
}

.amount-btn.active,
.amount-btn.featured {
    background: #2563eb;
    border-color: #2563eb;
    color: white;
}

.amount-btn.featured {
    position: relative;
    overflow: hidden;
}

.amount-btn.featured::before {
    content: 'Popular';
    position: absolute;
    top: 0;
    right: 0;
    background: #fbbf24;
    color: #1f2937;
    font-size: 0.6rem;
    padding: 0.2rem 0.4rem;
    border-radius: 0 8px 0 8px;
    font-weight: 700;
}

.custom-amount-input {
    margin-top: 1rem;
}

.custom-amount-input input {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid #e5e7eb;
    border-radius: 10px;
    font-size: 1rem;
    transition: border-color 0.3s ease;
}

.custom-amount-input input:focus {
    outline: none;
    border-color: #2563eb;
}

.donation-form {
    margin-bottom: 2rem;
}

.donate-now-btn {
    width: 100%;
    background: linear-gradient(135deg, #2563eb, #1d4ed8);
    color: white;
    border: none;
    border-radius: 15px;
    padding: 1rem 2rem;
    font-size: 1.125rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.donate-now-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 15px -3px rgb(37 99 235 / 0.4);
}

.donate-now-btn:active {
    transform: translateY(0);
}

.secure-text {
    text-align: center;
    font-size: 0.8rem;
    color: #6b7280;
    margin: 0;
}

.share-project {
    border-top: 1px solid #e5e7eb;
    padding-top: 2rem;
}

.share-project h4 {
    font-size: 1rem;
    font-weight: 600;
    color: #1f2937;
    margin-bottom: 1rem;
    text-align: center;
}

.share-buttons {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem;
}

.share-btn {
    background: #f8fafc;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    padding: 0.5rem;
    font-size: 0.8rem;
    cursor: pointer;
    transition: all 0.3s ease;
    text-align: center;
}

.share-btn:hover {
    background: #eff6ff;
    border-color: #2563eb;
    color: #2563eb;
}

.share-btn.facebook:hover {
    background: #eff6ff;
    border-color: #4267b2;
    color: #4267b2;
}

.share-btn.twitter:hover {
    background: #f0f9ff;
    border-color: #1da1f2;
    color: #1da1f2;
}

.share-btn.whatsapp:hover {
    background: #f0fdf4;
    border-color: #25d366;
    color: #25d366;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .content-grid {
        grid-template-columns: 1fr;
        gap: 3rem;
    }
    
    .sidebar {
        order: -1;
    }
    
    .sticky {
        position: static;
    }
    
    .donation-widget {
        max-width: 500px;
        margin: 0 auto;
    }
}

@media (max-width: 768px) {
    .hero-stats {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .stat-item {
        padding: 1rem;
    }
    
    .stat-number {
        font-size: 1.5rem;
    }
    
    .project-hero-title {
        font-size: 2.5rem;
    }
    
    .project-hero-subtitle {
        font-size: 1.25rem;
    }
    
    .main-content {
        padding: 2rem;
    }
    
    .impact-grid {
        grid-template-columns: 1fr;
    }
    
    .amount-buttons {
        grid-template-columns: repeat(2, 1fr);
    }
    
    .share-buttons {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 480px) {
    .project-detail-hero {
        height: 50vh;
        min-height: 400px;
    }
    
    .hero-content {
        padding: 1rem;
    }
    
    .container {
        padding: 0 1rem;
    }
    
    .main-content {
        padding: 1.5rem;
    }
    
    .donation-widget {
        padding: 1.5rem;
    }
    
    .amount-buttons {
        grid-template-columns: 1fr;
    }
}

/* Animation for page load */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.main-content,
.donation-widget {
    animation: fadeInUp 0.6s ease forwards;
}

.donation-widget {
    animation-delay: 0.2s;
}

/* Loading states */
.donate-now-btn.loading {
    opacity: 0.7;
    pointer-events: none;
}

.donate-now-btn.loading::after {
    content: '';
    width: 16px;
    height: 16px;
    border: 2px solid transparent;
    border-top: 2px solid currentColor;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin-left: 0.5rem;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Amount button selection
    const amountButtons = document.querySelectorAll('.amount-btn');
    const customInput = document.querySelector('.custom-amount-input');
    const customInputField = customInput.querySelector('input');
    const donateButton = document.querySelector('.donate-now-btn');
    
    let selectedAmount = 100; // Default featured amount
    
    amountButtons.forEach(button => {
        button.addEventListener('click', function() {
            // Remove active class from all buttons
            amountButtons.forEach(btn => btn.classList.remove('active'));
            
            if (this.classList.contains('custom')) {
                this.classList.add('active');
                customInput.style.display = 'block';
                customInputField.focus();
                selectedAmount = 0;
            } else {
                this.classList.add('active');
                customInput.style.display = 'none';
                selectedAmount = parseInt(this.dataset.amount);
            }
            
            updateDonateButton();
        });
    });
    
    // Custom amount input
    customInputField.addEventListener('input', function() {
        selectedAmount = parseInt(this.value) || 0;
        updateDonateButton();
    });
    
    function updateDonateButton() {
        const btnText = donateButton.querySelector('span');
        if (selectedAmount > 0) {
            btnText.textContent = `Donate ${selectedAmount}`;
            donateButton.disabled = false;
        } else {
            btnText.textContent = 'Enter Amount';
            donateButton.disabled = true;
        }
    }
    
    // Donate button click
    donateButton.addEventListener('click', function() {
        if (selectedAmount > 0) {
            this.classList.add('loading');
            this.querySelector('span').textContent = 'Processing';
            
            // Simulate donation process (remove in production)
            setTimeout(() => {
                this.classList.remove('loading');
                this.querySelector('span').textContent = `Donate ${selectedAmount}`;
                alert(`Thank you for your ${selectedAmount} donation!`);
            }, 2000);
        }
    });
    
    // Share functionality
    const shareButtons = document.querySelectorAll('.share-btn');
    shareButtons.forEach(button => {
        button.addEventListener('click', function() {
            const projectTitle = document.querySelector('.project-hero-title').textContent;
            const projectUrl = window.location.href;
            
            if (this.classList.contains('facebook')) {
                window.open(`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(projectUrl)}`, '_blank');
            } else if (this.classList.contains('twitter')) {
                window.open(`https://twitter.com/intent/tweet?text=${encodeURIComponent(projectTitle)}&url=${encodeURIComponent(projectUrl)}`, '_blank');
            } else if (this.classList.contains('whatsapp')) {
                window.open(`https://wa.me/?text=${encodeURIComponent(projectTitle + ' ' + projectUrl)}`, '_blank');
            } else if (this.classList.contains('copy')) {
                navigator.clipboard.writeText(projectUrl).then(() => {
                    this.textContent = '✓ Copied!';
                    setTimeout(() => {
                        this.textContent = '🔗 Copy Link';
                    }, 2000);
                });
            }
        });
    });
    
    // Initialize with featured amount
    document.querySelector('.amount-btn.featured').classList.add('active');
    updateDonateButton();
    
    // Smooth scroll for breadcrumb links
    document.querySelectorAll('.breadcrumb a[href^="#"]').forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({ behavior: 'smooth' });
            }
        });
    });
    
    // Parallax effect for hero background
    window.addEventListener('scroll', function() {
        const scrolled = window.pageYOffset;
        const heroBackground = document.querySelector('.hero-background img');
        if (heroBackground) {
            heroBackground.style.transform = `translateY(${scrolled * 0.5}px)`;
        }
    });
});
</script>